const APP_CONSTANTS = {
  apiPrefix: '',
  params: 'params',
  query: 'query',
  body: 'body',
  file: 'file',
  service: 'express-template',
};

const DEFAULT_PAGING = {
  limit: 100,
  skip: 0,
};

export { APP_CONSTANTS, DEFAULT_PAGING };
